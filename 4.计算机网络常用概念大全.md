
# 4.1 计算机网络体系结构
# 4.2 物理层
# 4.3 数据链路层
# 4.4 网络层
# 4.5 传输层
  - 发送窗口：发送方维护的一个发送数据的滑动窗口
	​[已发已确认] | <左边界 [ 已发未确认 | 可发未发送 ] 右边界> | [暂时不能发]
	​发送窗口的大小 = 第 2 段长度 + 第 3 段长度 = \min(cwnd, rwnd)。
	​可用窗口的大小 = 仅指第 3 段的长度。
- 数据偏移（TCP报文段首部部分的字段）：即首部长度，单位为4B，因此该字段*4得到的值就是首部长度，并非偏移量。
# 4.6 应用层
